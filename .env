# Use relative path for development (Next.js will rewrite to backend)
# For production, this can be overridden with the full URL
# IMPORTANT: Trailing slash is required for proper URL concatenation
NEXT_PUBLIC_BASE_URL=/api/